CREATE TABLE target_rate (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  currency_entity_id BIGINT NOT NULL,
  target INTEGER NOT NULL,
  rate DECIMAL(19, 2) NOT NULL,
  CONSTRAINT pk_target_rate PRIMARY KEY (id)
);

ALTER TABLE target_rate ADD CONSTRAINT FK_TARGET_RATE_ON_CURRENCY_ENTITY FOREIGN KEY (currency_entity_id) REFERENCES currency_rate (id);