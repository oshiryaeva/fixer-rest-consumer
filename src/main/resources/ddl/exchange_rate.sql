CREATE TABLE exchange_rate (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  currency_entity_id BIGINT NOT NULL,
  target INTEGER NOT NULL,
  rate DECIMAL(19, 2) NOT NULL,
  CONSTRAINT pk_exchange_rate PRIMARY KEY (id)
);

ALTER TABLE exchange_rate ADD CONSTRAINT FK_EXCHANGE_RATE_ON_CURRENCY_ENTITY FOREIGN KEY (currency_entity_id) REFERENCES currency_entity (id);